{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.5.2",
    "pug": "^3.0.2",
    "pug-plain-loader": "^1.1.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "sass": "^1.63.6",
    "sass-loader": "^13.3.2"
  }
}
